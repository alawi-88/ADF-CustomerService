# Customer-experience analytics dashboard
# All dependencies run locally. Optional Groq cloud LLM via GROQ_API_KEY.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
jinja2>=3.1
python-multipart>=0.0.9
pandas>=2.0,<3.0
numpy>=1.24,<3.0
openpyxl>=3.1,<4.0
pyarrow>=15.0
scikit-learn>=1.3
requests>=2.31

# Ollama is OPTIONAL. The app degrades gracefully if it is not running.
# Install Ollama on the VM (https://ollama.com), then:
#   ollama pull qwen2.5:7b-instruct
